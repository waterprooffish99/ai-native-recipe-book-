---
id: "014"
title: "Phase 10 Stage 2: Serving Size Smart Scaling Controller and Checklist Integration"
stage: "green"
date: "2026-06-23"
surface: "agent"
model: "gemini-3.5-flash"
feature: "001-recipe-content-schema"
branch: "001-recipe-content-schema"
user: "waterprooffish99"
command: "npx tsc --noEmit"
labels: ["Scaling", "RecipeDetail", "IngredientChecklist", "TypeScript"]
links:
  spec: "specs/001-recipe-content-schema/spec.md"
  ticket: null
  adr: null
  pr: null
files:
  - frontend/src/components/recipes/ServingSizeController.tsx
  - frontend/src/components/recipes/IngredientChecklist.tsx
  - frontend/src/components/recipes/RecipeDetail.tsx
tests:
  - npx tsc --noEmit
---

## Prompt

```
// EXECUTION DIRECTIVE: GLOBAL PLATE PHASE 10 STAGE 2 (T147-T151)
// TARGET_DIR: /home/waterprooffish99/projects/recipe-cook-book/
// MODE: FULL-STACK FEATURE INJECTION & MATH SCALING

system_context {
  project_state: "Phase 10 Stage 1 COMPLETE. TypeScript is strict and passing. Cmd+K is live."
  guardrails: "Strict Spec-Driven Development. Maintain type safety. Do not break existing progress checkbox state."
}

execute_sequence {
  
  step_1_backend_scaling_models {
    description: "Prepare the backend to support scaling logic if required by the API."
    actions: [
      "READ backend/src/models/recipe.py.",
      "ENSURE the Recipe model clearly exposes the 'base_servings' integer.",
      "CREATE any necessary backend utilities or route parameters (e.g., ?servings=X) if the spec dictates server-side scaling, OR explicitly state that scaling will be handled via client-side state manipulation to save API calls."
    ]
  }

  step_2_frontend_scaling_controller {
    description: "Build the React UI to allow users to adjust serving sizes."
    actions: [
      "CREATE frontend/src/components/recipes/ServingSizeController.tsx.",
      "IMPLEMENT a clean, dark-mode toggle layout (e.g., a '-' button, the current number, and a '+' button).",
      "INJECT this component into frontend/src/components/recipes/RecipeDetail.tsx, ideally placed near the recipe metadata (Time, Difficulty, Servings)."
    ]
  }

  step_3_dynamic_ingredient_math {
    description: "Wire the controller to the ingredient list to recalculate volumes."
    actions: [
      "READ frontend/src/components/recipes/IngredientChecklist.tsx.",
      "IMPLEMENT a scaling multiplier: (target_servings / base_servings).",
      "MULTIPLY the numeric quantities of each ingredient by this factor before rendering them to the screen.",
      "Ensure the math handles fractions/decimals cleanly (e.g., rounding to 1 or 2 decimal places to avoid 1.333333 cups)."
    ]
  }

  step_4_status_report {
    description: "Verify TypeScript compilation and math logic."
    output_format: "markdown"
    requirements: [
      "Confirm 'npx tsc --noEmit' passes with 0 errors.",
      "Detail whether scaling is handled client-side or server-side.",
      "Instruct the user to open the Sajji recipe and click the '+' button to verify the ingredient quantities scale mathematically."
    ]
  }
}
```

## Response snapshot

Created the `ServingSizeController.tsx` layout toggle in the frontend, exported the `scaleQuantity` utility helper in `IngredientChecklist.tsx` to handle parsing of numeric and fraction quantities cleanly, and wired the resulting servings scale multiplier to both the checklists and plain text fallbacks inside `RecipeDetail.tsx`. Verified clean compilation status under `npx tsc --noEmit`.

## Outcome

- ✅ Impact: Ingredient scaling handles fractions/decimals in real-time client-side, providing instant user interaction without server calls.
- 🧪 Tests: npx tsc --noEmit completed with 0 errors.
- 📁 Files: Created `ServingSizeController.tsx`, modified `IngredientChecklist.tsx`, `RecipeDetail.tsx`.
- 🔁 Next prompts: Phase 10 Stage 3: PWA Offline Support.
- 🧠 Reflection: Processing scaling client-side provides instant feedback, preserving local checklist state. Parsing string representation of fractions (`1/2`) allows parsing traditional cookbooks.

## Evaluation notes (flywheel)

- Failure modes observed: None
- Graders run and results (PASS/FAIL): PASS
- Prompt variant (if applicable): null
- Next experiment (smallest change to try): Add PWA caching rules.
